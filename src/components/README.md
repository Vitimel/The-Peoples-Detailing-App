# Components

The converted prototype still keeps most visual primitives inside `src/screens/PrototypeApp.jsx`
so the first Vite handoff stays faithful to the approved one-file source. Future cleanup should
move repeated primitives such as `Icon`, `HeaderBar`, `Row`, `Field`, and navigation shells here
only after visual parity is confirmed.
